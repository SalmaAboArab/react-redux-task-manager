// Expo's default entry point (node_modules/expo/AppEntry.js) looks for
// App.js at the project root. The real component lives at src/App.js to
// match this repo's structure — this file just re-exports it.
export { default } from './src/App';
